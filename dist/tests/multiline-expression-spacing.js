"use strict";
exports.__esModule = true;
// O
var o = Math.random() > 0.5
    ? "Y"
    : "N".indexOf("o");
[
    Math.random() > 0.5
        ? []
        : [],
    1
].push([].sort()
    .sort());
console.log({
    x: Math.random() > 0.5
        ? "y"
        : "n",
    y: []
        .sort()
        .sort(),
    z: true
}, Math.random() > 0.5
    ? 1 : 2, 3);
var o2 = [1]
    .length;
// X
var x = Math.random() > 0.5
    ? "Y"
    : "N".indexOf("x");
[
    Math.random() > 0.5
        ? []
        : [],
    1
].sort()
    .sort();
console.log({
    x: Math.random() > 0.5
        ? "y"
        : "n",
    y: []
        .sort()
        .sort(),
    z: true
}, Math.random() > 0.5
    ? 1 : 2, 3);
var x2 = [1]
    .length;
if ([].length
    && [].length) {
    console.log(1);
}
