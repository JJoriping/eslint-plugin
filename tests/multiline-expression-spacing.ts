// O
const o = Math.random() > 0.5
  ? "Y"
  : "N".indexOf(
    "o"
  )
;
[
  Math.random() > 0.5
    ? []
    : []
  ,
  1
].push(
  [].sort()
    .sort()
);
console.log(
  {
    x: Math.random() > 0.5
      ? "y"
      : "n"
    ,
    y: []
      .sort()
      .sort()
    ,
    z: true
  },
  Math.random() > 0.5
    ? 1
    : 2
  , 3
);
const o2 = [ 1 ]
  .length
;

// X
const x = Math.random() > 0.5
  ? "Y"
  : "N".indexOf(
    "x"
  );
[
  Math.random() > 0.5
    ? []
    : []
  ,
  1
].sort()
  .sort();
console.log(
  {
    x: Math.random() > 0.5
      ? "y"
      : "n",
    y: []
      .sort()
      .sort(),
    z: true
  },
  Math.random() > 0.5
    ? 1
    : 2,
  3
);
const x2 = [ 1 ]
  .length
;
if([].length
  && [].length){
  console.log(1);
}
const x3 = void console.log(
  1,
  [])
;
type X = 1
  | 2
  | 3;

export {};